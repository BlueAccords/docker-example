machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install --upgrade docker-compose==1.8.0

test:
  override:
    - docker-compose run -d --no-deps web
    - cd app; mocha

deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY
